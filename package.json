{
	"name": "server",
	"version": "1.0.0",
	"main": "server",
	"license": "MIT",
	"dependencies": {
		"express": "^4.17.1",
		"path": "^0.12.7"
	},
	"scripts": {
		"start": "node server.js",
		"heroku-postbuild": "cd client && yarn && yarn run build"
	}
}
